/**
 * Blog post asset (as returned from API)
 */
export interface BlogAsset {
  path: string; // URL path for display
  type: string; // MIME type (e.g., "image/jpeg", "application/pdf", "video/mp4")
}

